{
  "name": "gaik-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter=gaik-dashboard dev",
    "dashboard:build": "pnpm --filter=gaik-dashboard build",
    "dashboard:docker": "docker build -f web/dashboard/Dockerfile -t gaik-dashboard ."
  }
}